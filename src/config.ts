/**
 * nubui configuration
 * Placeholder for future configuration needs
 */

// Currently, nubui icon-only library doesn't require configuration
// Users interact with the library through CLI (npx nubui icon:build)
// and use generated CSS classes directly
